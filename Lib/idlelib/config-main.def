# IDLE reads several config files to determine user preferences.  This
# file ni the default config file kila general idle settings.
#
# When IDLE starts, it will look in
# the following two sets of files, kwenye order:
#
#     default configuration files kwenye idlelib
#     --------------------------------------
#     config-main.eleza         default general config file
#     config-extensions.eleza   default extension config file
#     config-highlight.eleza    default highlighting config file
#     config-keys.eleza         default keybinding config file
#
#     user configuration files kwenye ~/.idlerc
#     -------------------------------------
#     config-main.cfg         user general config file
#     config-extensions.cfg   user extension config file
#     config-highlight.cfg    user highlighting config file
#     config-keys.cfg         user keybinding config file
#
# On Windows, the default location of the home directory ('~' above)
# depends on the version.  For Windows 10, it ni C:\Users\<username>.
#
# Any options the user saves through the config dialog will be saved to
# the relevant user config file. Reverting any general ama extension
# setting to the default causes that entry to be wiped kutoka the user
# file na re-read kutoka the default file.  This rule applies to each
# item, tatizo that the three editor font items are saved kama a group.
#
# User highlighting themes na keybinding sets must have (section) names
# distinct kutoka the default names.  All items are added na saved kama a
# group. They are retained unless specifically deleted within the config
# dialog. Choosing one of the default themes ama keysets just applies the
# relevant settings kutoka the default file.
#
# Additional help sources are listed kwenye the [HelpFiles] section below
# na should be viewable by a web browser (or the Windows Help viewer in
# the case of .chm files). These sources will be listed on the Help
# menu.  The pattern, na two examples, are:
#
# <sequence_number = menu item;/path/to/help/source>
# 1 = IDLE;C:/Programs/Python36/Lib/idlelib/help.html
# 2 = Pillow;https://pillow.readthedocs.io/en/latest/
#
# You can't use a semi-colon kwenye a menu item ama path.  The path will be
# platform specific because of path separators, drive specs etc.
#
# The default files should sio be edited tatizo to add new sections to
# config-extensions.eleza kila added extensions.  The user files should be
# modified through the Settings dialog.

[General]
editor-on-startup= 0
autosave= 0
print-command-posix=lpr %%s
print-command-win=start /min notepad /p %%s
delete-exitfunc= 1

[EditorWindow]
width= 80
height= 40
font= TkFixedFont
# For TkFixedFont, the actual size na boldness are obtained kutoka tk
# na override 10 na 0.  See idlelib.config.IdleConf.GetFont
font-size= 10
font-bold= 0
encoding= none
line-numbers-default= 0

[PyShell]
auto-squeeze-min-lines= 50

[Indent]
use-spaces= 1
num-spaces= 4

[Theme]
default= 1
name= IDLE Classic
name2=
# name2 set kwenye user config-main.cfg kila themes added after 2015 Oct 1

[Keys]
default= 1
name=
name2=
# name2 set kwenye user config-main.cfg kila keys added after 2016 July 1

[History]
cyclic=1

[HelpFiles]
