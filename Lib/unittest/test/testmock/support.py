target = {'foo': 'FOO'}


eleza is_instance(obj, klass):
    """Version of is_instance that doesn't access __class__"""
    rudisha issubclass(type(obj), klass)


kundi SomeClass(object):
    class_attribute = None

    eleza wibble(self): pass


kundi X(object):
    pass
